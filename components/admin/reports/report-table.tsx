"use client";
import { MoreHorizontal } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import type { Report } from "@/lib/mock-data/reports";
export function ReportTable({ reports, onPreview, onAction }: { reports: Report[]; onPreview: (report: Report) => void; onAction: (report: Report, action: string) => void }) { return <Table><TableHeader className="sticky top-0 z-10 bg-white"><TableRow><TableHead>Report name</TableHead><TableHead>Category</TableHead><TableHead>Generated by</TableHead><TableHead>Date</TableHead><TableHead>Format</TableHead><TableHead>Status</TableHead><TableHead>Actions</TableHead></TableRow></TableHeader><TableBody>{reports.map((report) => <TableRow key={report.id}><TableCell><button className="text-left font-semibold text-indigo-700 hover:underline" onClick={() => onPreview(report)}>{report.name}</button></TableCell><TableCell className="text-xs text-muted">{report.category}</TableCell><TableCell>{report.generatedBy}</TableCell><TableCell className="whitespace-nowrap text-xs text-muted">{report.date}</TableCell><TableCell><Badge variant="info">{report.format}</Badge></TableCell><TableCell><Badge variant={report.status === "Ready" ? "success" : report.status === "Generating" ? "warning" : "default"}>{report.status}</Badge></TableCell><TableCell><details className="relative"><summary className="inline-flex list-none"><Button variant="ghost" size="icon" aria-label={`Actions for ${report.name}`}><MoreHorizontal className="h-4 w-4" /></Button></summary><div className="absolute right-0 z-20 mt-1 w-36 rounded-xl border border-line bg-white p-1 shadow-lg">{["Preview", "Download", "Regenerate", "Delete"].map((action) => <button key={action} className="block w-full rounded-lg px-3 py-2 text-left text-xs text-ink hover:bg-slate-50" onClick={() => action === "Preview" ? onPreview(report) : onAction(report, action)}>{action}</button>)}</div></details></TableCell></TableRow>)}</TableBody></Table>; }
