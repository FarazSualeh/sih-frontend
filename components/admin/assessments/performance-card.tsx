import { Card } from "@/components/ui/card";
export function PerformanceCard({ department, studentsAssigned, completion, averageScore, highestScore, pendingStudents }: { department: string; studentsAssigned: number; completion: number; averageScore: number; highestScore: number; pendingStudents: number }) { return <Card className="p-5 transition hover:-translate-y-1 hover:shadow-md"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold text-ink">{department}</p><p className="mt-1 text-xs text-muted">{studentsAssigned.toLocaleString()} students assigned</p></div><span className="rounded-lg bg-indigo-50 px-2 py-1 text-xs font-bold text-indigo-700">{completion}%</span></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-indigo-500" style={{ width: `${completion}%` }} /></div><div className="mt-4 grid grid-cols-3 gap-2 text-xs"><span className="text-muted">Average <strong className="text-ink">{averageScore}%</strong></span><span className="text-muted">Highest <strong className="text-ink">{highestScore}%</strong></span><span className="text-right text-muted">Pending <strong className="text-orange-600">{pendingStudents}</strong></span></div></Card>; }
