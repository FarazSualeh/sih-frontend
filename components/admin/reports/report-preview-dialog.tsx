"use client";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import type { Report } from "@/lib/mock-data/reports";
export function ReportPreviewDialog({ report, open, onOpenChange }: { report?: Report; open: boolean; onOpenChange: (open: boolean) => void }) { if (!report) return null; return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-h-[90vh] overflow-y-auto sm:max-w-3xl"><DialogHeader><div className="flex items-start justify-between gap-3"><div><DialogTitle>{report.name}</DialogTitle><DialogDescription>Institutional report preview · {report.date}</DialogDescription></div><Badge variant="success">{report.status}</Badge></div></DialogHeader><div className="space-y-5"><div className="rounded-2xl bg-indigo-950 p-6 text-white"><p className="text-xs uppercase tracking-[0.18em] text-indigo-200">SkillConnect Insights</p><h3 className="mt-3 text-2xl font-semibold">{report.name}</h3><p className="mt-2 text-sm text-indigo-100">{report.description}</p></div><div className="grid gap-4 sm:grid-cols-3">{[["Students analysed", "12,584"], ["Average readiness", "78%"], ["Placement rate", "72%"]].map(([label, value]) => <div key={label} className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-muted">{label}</p><p className="mt-2 text-xl font-semibold text-ink">{value}</p></div>)}</div><Section title="Executive summary">Readiness improved across core technical cohorts while cloud and communication remain the largest areas for targeted support.</Section><Section title="Department statistics">AI & DS leads placement outcomes at 88%, followed by Computer Engineering at 82%.</Section><Section title="Skill readiness snapshot">Python, SQL, and Communication have the strongest student coverage. Cloud Computing has the highest demand gap.</Section><Section title="Placement & assessment summary">8,942 applications progressed through the current cycle, with 1,098 students placed. Assessment completion is 84%.</Section><Section title="Top skill gaps & industry demand">Cloud Computing · Machine Learning · Docker & Kubernetes are most frequently requested in active roles.</Section><Section title="AI recommendations">Increase cloud assessments, add communication practice for Mechanical cohorts, and create a Prompt Engineering pathway.</Section><p className="border-t border-line pt-4 text-xs text-muted">Generated by {report.generatedBy} · {report.date} · Mock report preview</p></div><DialogFooter><Button onClick={() => onOpenChange(false)}>Close Preview</Button></DialogFooter></DialogContent></Dialog>; }
function Section({ title, children }: { title: string; children: React.ReactNode }) { return <div><p className="text-xs font-semibold uppercase tracking-[0.12em] text-muted">{title}</p><p className="mt-2 text-sm leading-6 text-ink">{children}</p></div>; }
