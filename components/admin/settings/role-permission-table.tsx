"use client";
import { useState } from "react";
import type { PermissionRow } from "@/lib/mock-data/settings";
export function RolePermissionTable({ rows }: { rows: PermissionRow[] }) { const [items, setItems] = useState(rows); return <div className="overflow-x-auto"><table className="w-full min-w-[680px] text-sm"><thead className="border-b border-line bg-slate-50"><tr><th className="px-4 py-3 text-left text-xs uppercase tracking-[0.1em] text-muted">Permission</th>{["Admin", "Academician", "Industry", "Student"].map((role) => <th key={role} className="px-4 py-3 text-center text-xs uppercase tracking-[0.1em] text-muted">{role}</th>)}</tr></thead><tbody>{items.map((row, index) => <tr key={row.permission} className="border-b border-line last:border-0"><td className="px-4 py-3 font-medium text-ink">{row.permission}</td>{(["admin", "academician", "industry", "student"] as const).map((role) => <td key={role} className="px-4 py-3 text-center"><input type="checkbox" checked={row[role]} onChange={(event) => setItems((current) => current.map((item, itemIndex) => itemIndex === index ? { ...item, [role]: event.target.checked } : item))} className="h-4 w-4 accent-indigo-600" aria-label={`${row.permission} for ${role}`} /></td>)}</tr>)}</tbody></table></div>; }
