"use client";
import { useState } from "react";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import type { Assessment, AssessmentStatus, Difficulty } from "@/lib/mock-data/assessments";
export function CreateAssessmentDialog({ open, onOpenChange, assessment, onSave }: { open: boolean; onOpenChange: (open: boolean) => void; assessment?: Assessment; onSave: (assessment: Assessment) => void }) { const [title, setTitle] = useState(assessment?.title ?? ""); const [skill, setSkill] = useState(assessment?.skill ?? "Python"); const [description, setDescription] = useState(assessment?.description ?? ""); const [difficulty, setDifficulty] = useState<Difficulty>(assessment?.difficulty ?? "Beginner"); const [duration, setDuration] = useState(assessment?.duration.toString() ?? "45"); const [passingMarks, setPassingMarks] = useState(assessment?.passingMarks.toString() ?? "60"); const [status, setStatus] = useState<AssessmentStatus>(assessment?.status ?? "Draft"); const submit = (event: React.FormEvent) => { event.preventDefault(); onSave({ ...(assessment ?? { id: `assessment-${Date.now()}`, questions: 20, participants: 0, completionRate: 0, averageScore: 0, attemptsAllowed: 1, createdBy: "SkillConnect Admin", assignedDepartments: ["Computer Engineering"], assignedStudents: 0, topPerformers: [], createdDate: "Today" }), title, skill, description, category: "Technical", difficulty, duration: Number(duration), passingMarks: Number(passingMarks), status }); onOpenChange(false); }; return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-h-[90vh] overflow-y-auto sm:max-w-2xl"><DialogHeader><DialogTitle>{assessment ? "Edit assessment" : "Create assessment"}</DialogTitle><DialogDescription>Configure an assessment for future AI-assisted evaluation.</DialogDescription></DialogHeader><form onSubmit={submit} className="space-y-4"><label className="block space-y-1.5 text-sm font-medium">Assessment title<Input required value={title} onChange={(e) => setTitle(e.target.value)} placeholder="e.g. Python Fundamentals" /></label><label className="block space-y-1.5 text-sm font-medium">Description<textarea required value={description} onChange={(e) => setDescription(e.target.value)} className="min-h-24 w-full rounded-xl border border-line px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-indigo-300" /></label><div className="grid gap-4 sm:grid-cols-2"><Field label="Category"><select className="field" defaultValue="Technical"><option>Technical</option><option>Soft Skills</option><option>Domain</option><option>Emerging</option></select></Field><Field label="Target skill"><Input value={skill} onChange={(e) => setSkill(e.target.value)} /></Field><Field label="Difficulty"><select className="field" value={difficulty} onChange={(e) => setDifficulty(e.target.value as Difficulty)}><option>Beginner</option><option>Intermediate</option><option>Advanced</option></select></Field><Field label="Duration (minutes)"><Input type="number" value={duration} onChange={(e) => setDuration(e.target.value)} /></Field><Field label="Passing marks"><Input type="number" value={passingMarks} onChange={(e) => setPassingMarks(e.target.value)} /></Field><Field label="Maximum attempts"><Input type="number" defaultValue={assessment?.attemptsAllowed ?? 1} /></Field><Field label="Assign department"><select className="field"><option>Computer Engineering</option><option>AI & DS</option><option>IT</option><option>Electronics</option></select></Field><Field label="Assign semester"><select className="field"><option>Semester 7</option><option>Semester 6</option><option>Semester 5</option></select></Field></div><label className="flex items-center gap-2 rounded-xl border border-line p-3 text-sm"><input type="checkbox" checked={status === "Active"} onChange={(e) => setStatus(e.target.checked ? "Active" : "Draft")} className="h-4 w-4 accent-indigo-600" />Publish assessment immediately</label><DialogFooter><Button variant="outline" onClick={() => onOpenChange(false)}>Cancel</Button><Button type="submit">Save assessment</Button></DialogFooter></form></DialogContent></Dialog>; }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="space-y-1.5 text-sm font-medium">{label}{children}</label>; }
