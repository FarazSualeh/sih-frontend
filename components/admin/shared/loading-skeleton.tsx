export function LoadingSkeleton({ type = "card", count = 1 }: { type?: "card" | "table" | "chart" | "dialog"; count?: number }) { return <div className={type === "table" ? "space-y-3" : type === "chart" ? "h-72 rounded-2xl bg-slate-100 animate-pulse" : "space-y-3 rounded-2xl bg-slate-100 p-5 animate-pulse"}>{Array.from({ length: count }, (_, index) => <div key={index} className={type === "table" ? "h-10 rounded-lg bg-slate-100 animate-pulse" : "h-4 w-2/3 rounded bg-slate-200"} />)}</div>; }
