"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import type { InstitutionSettings } from "@/lib/mock-data/settings";
export function InstitutionSettingsForm({ initial }: { initial: InstitutionSettings }) { const [form, setForm] = useState(initial); const update = (key: keyof InstitutionSettings, value: string) => setForm((current) => ({ ...current, [key]: value })); return <div className="space-y-4"><div className="grid gap-4 sm:grid-cols-2"><label className="space-y-1.5 text-sm font-medium">Institution name<Input value={form.institutionName} onChange={(e) => update("institutionName", e.target.value)} /></label><label className="space-y-1.5 text-sm font-medium">Email<Input type="email" value={form.email} onChange={(e) => update("email", e.target.value)} /></label><label className="space-y-1.5 text-sm font-medium">Phone<Input value={form.phone} onChange={(e) => update("phone", e.target.value)} /></label><label className="space-y-1.5 text-sm font-medium">Website<Input value={form.website} onChange={(e) => update("website", e.target.value)} /></label></div><label className="block space-y-1.5 text-sm font-medium">Address<Input value={form.address} onChange={(e) => update("address", e.target.value)} /></label><div className="grid gap-4 sm:grid-cols-2"><label className="space-y-1.5 text-sm font-medium">Departments<Input value={form.departments.join(", ")} onChange={(e) => update("departments", e.target.value)} /></label><label className="space-y-1.5 text-sm font-medium">Academic year<select className="h-10 w-full rounded-xl border border-line bg-white px-3 text-sm" value={form.academicYear} onChange={(e) => update("academicYear", e.target.value)}><option>2026-27</option><option>2027-28</option></select></label></div><div className="flex items-center gap-3"><div className="flex h-16 w-16 items-center justify-center rounded-xl border border-dashed border-line text-xs text-muted">Logo</div><Button variant="outline">Upload Logo</Button><Button onClick={() => undefined}>Save Institution Settings</Button></div></div>; }
