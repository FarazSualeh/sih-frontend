"use client";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Badge } from "@/components/ui/badge";
import type { Question } from "@/lib/mock-data/assessments";
export function QuestionManagerDialog({ question, open, onOpenChange }: { question?: Question; open: boolean; onOpenChange: (open: boolean) => void }) { if (!question) return null; return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent><DialogHeader><DialogTitle>Question preview</DialogTitle><DialogDescription>{question.skill} · {question.type}</DialogDescription></DialogHeader><div className="space-y-4 text-sm"><p className="font-medium leading-6 text-ink">{question.text}</p><div className="space-y-2">{question.options.map((option) => <div key={option} className={`rounded-xl border p-3 ${option === question.correctAnswer ? "border-emerald-300 bg-emerald-50" : "border-line"}`}>{option}</div>)}</div><div className="flex flex-wrap gap-2"><Badge>{question.difficulty}</Badge><Badge variant="info">{question.marks} marks</Badge><Badge variant="default">{question.timeLimit} min</Badge></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs font-semibold uppercase tracking-[0.12em] text-muted">Explanation</p><p className="mt-2 leading-6 text-muted">{question.explanation}</p></div></div></DialogContent></Dialog>; }
