"use client";
import { Bell, CheckCheck } from "lucide-react";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { notifications as initialNotifications } from "@/lib/mock-data/notifications";
export function NotificationPanel() { const [items, setItems] = useState(initialNotifications); const unread = items.filter((item) => item.unread).length; return <details className="relative"><summary className="flex list-none items-center"><button className="relative rounded-xl border border-line bg-white p-2.5 text-muted transition hover:bg-[#f4f5f0]" aria-label="Open notifications"><Bell className="h-4 w-4" />{unread > 0 && <span className="absolute -right-1 -top-1 flex h-4 min-w-4 items-center justify-center rounded-full bg-coral px-1 text-[0.6rem] font-bold text-white">{unread}</span>}</button></summary><div className="absolute right-0 z-30 mt-3 w-[min(22rem,calc(100vw-2rem))] rounded-2xl border border-line bg-white p-4 shadow-xl"><div className="flex items-center justify-between"><p className="font-semibold text-ink">Notifications</p><Button variant="ghost" size="sm" onClick={() => setItems((current) => current.map((item) => ({ ...item, unread: false })))}><CheckCheck className="h-3.5 w-3.5" />Mark all read</Button></div><div className="mt-3 space-y-1">{items.map((item) => <div key={item.id} className={`rounded-xl p-3 ${item.unread ? "bg-indigo-50" : "bg-white"}`}><div className="flex gap-2"><span className={`mt-1 h-2 w-2 shrink-0 rounded-full ${item.tone === "warning" ? "bg-amber-500" : item.tone === "success" ? "bg-emerald-500" : "bg-indigo-500"}`} /><div><p className="text-sm font-semibold text-ink">{item.title}</p><p className="mt-0.5 text-xs leading-5 text-muted">{item.detail}</p><p className="mt-1 text-[0.65rem] text-muted">{item.time}</p></div></div></div>)}</div></div></details>; }
