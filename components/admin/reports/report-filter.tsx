import { Input } from "@/components/ui/input";
export function ReportFilter({ search, category, status, format, onSearch, onCategory, onStatus, onFormat }: { search: string; category: string; status: string; format: string; onSearch: (value: string) => void; onCategory: (value: string) => void; onStatus: (value: string) => void; onFormat: (value: string) => void }) { const select = (value: string, onChange: (value: string) => void, options: string[]) => <select value={value} onChange={(event) => onChange(event.target.value)} className="h-10 rounded-xl border border-line bg-white px-3 text-xs font-medium text-ink outline-none focus:ring-2 focus:ring-indigo-300">{options.map((option) => <option key={option}>{option}</option>)}</select>; return <div className="flex flex-wrap gap-2"><Input value={search} onChange={(event) => onSearch(event.target.value)} placeholder="Search reports..." className="w-full sm:w-64" />{select(category, onCategory, ["All categories", "Student Readiness Report", "Skill Gap Report", "Placement Report", "Industry Demand Report"])}{select(status, onStatus, ["All statuses", "Ready", "Generating", "Scheduled"])}{select(format, onFormat, ["All formats", "PDF", "CSV", "Excel"])}</div>; }
