import { BriefcaseBusiness, Building2, ClipboardCheck, FileText, Target, TrendingUp, Users } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
const icons = { users: Users, target: Target, briefcase: BriefcaseBusiness, trending: TrendingUp, clipboard: ClipboardCheck, building: Building2, award: FileText, arrow: TrendingUp };
export function ReportCard({ report, onGenerate, onPreview }: { report: { name: string; description: string; icon: string; lastGenerated: string }; onGenerate: () => void; onPreview: () => void }) { const Icon = icons[report.icon as keyof typeof icons] ?? FileText; return <Card className="group p-5 transition hover:-translate-y-1 hover:shadow-md"><div className="flex items-start justify-between gap-3"><div className="rounded-xl bg-indigo-50 p-2.5 text-indigo-700"><Icon className="h-5 w-5" /></div><Badge variant="success">Available</Badge></div><p className="mt-5 font-semibold text-ink">{report.name}</p><p className="mt-2 min-h-12 text-sm leading-5 text-muted">{report.description}</p><p className="mt-4 text-xs text-muted">Last generated {report.lastGenerated}</p><div className="mt-4 flex gap-2"><Button size="sm" onClick={onGenerate}>Generate</Button><Button size="sm" variant="outline" onClick={onPreview}>Preview</Button></div></Card>; }
