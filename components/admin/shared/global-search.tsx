"use client";
import { Search } from "lucide-react";
import { useState } from "react";
const searchGroups = [{ group: "Students", results: ["Aarav Sharma", "Ishita Patel"] }, { group: "Skills", results: ["Python", "Cloud Computing"] }, { group: "Companies", results: ["TCS", "Infosys"] }, { group: "Reports", results: ["Quarterly Student Readiness"] }];
export function GlobalSearch() { const [query, setQuery] = useState(""); const groups = searchGroups.map((group) => ({ ...group, results: group.results.filter((result) => result.toLowerCase().includes(query.toLowerCase())) })).filter((group) => group.results.length); return <div className="relative hidden flex-1 justify-center md:flex"><Search className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search students, skills, companies, reports..." className="h-11 w-full max-w-md rounded-xl border border-line bg-white pl-10 pr-4 text-sm text-ink outline-none placeholder:text-muted focus:border-coral" />{query && <div className="absolute top-14 z-30 w-full max-w-md rounded-2xl border border-line bg-white p-3 shadow-xl">{groups.length ? groups.map((group) => <div key={group.group} className="mb-3 last:mb-0"><p className="px-2 text-[0.65rem] font-bold uppercase tracking-[0.12em] text-muted">{group.group}</p>{group.results.map((result) => <button key={result} className="mt-1 block w-full rounded-lg px-2 py-2 text-left text-sm text-ink hover:bg-slate-50">{result}</button>)}</div>) : <p className="p-2 text-sm text-muted">No search results</p>}</div>}</div>; }
