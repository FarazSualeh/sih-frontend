"use client";
import { MoreHorizontal } from "lucide-react";
import { AssessmentStatusBadge } from "@/components/admin/assessments/assessment-status-badge";
import { CompletionProgress } from "@/components/admin/assessments/completion-progress";
import { Button } from "@/components/ui/button";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import type { Assessment } from "@/lib/mock-data/assessments";
export function AssessmentTable({ assessments, onView, onEdit, onAction }: { assessments: Assessment[]; onView: (item: Assessment) => void; onEdit: (item: Assessment) => void; onAction: (item: Assessment, action: string) => void }) { return <Table><TableHeader className="sticky top-0 z-10 bg-white"><TableRow><TableHead>Assessment name</TableHead><TableHead>Category</TableHead><TableHead>Skill</TableHead><TableHead>Difficulty</TableHead><TableHead>Questions</TableHead><TableHead>Participants</TableHead><TableHead>Completion</TableHead><TableHead>Status</TableHead><TableHead>Actions</TableHead></TableRow></TableHeader><TableBody>{assessments.map((item) => <TableRow key={item.id}><TableCell><button onClick={() => onView(item)} className="max-w-48 text-left font-semibold text-indigo-700 hover:underline">{item.title}</button></TableCell><TableCell className="text-xs text-muted">{item.category}</TableCell><TableCell className="text-xs">{item.skill}</TableCell><TableCell><AssessmentStatusBadge status={item.difficulty} /></TableCell><TableCell>{item.questions}</TableCell><TableCell>{item.participants.toLocaleString()}</TableCell><TableCell><CompletionProgress value={item.completionRate} /></TableCell><TableCell><AssessmentStatusBadge status={item.status} /></TableCell><TableCell><details className="relative"><summary className="inline-flex list-none"><Button variant="ghost" size="icon" aria-label={`Actions for ${item.title}`}><MoreHorizontal className="h-4 w-4" /></Button></summary><div className="absolute right-0 z-20 mt-1 w-36 rounded-xl border border-line bg-white p-1 shadow-lg">{["View Details", "Edit", "Duplicate", "Archive"].map((action) => <button key={action} className="block w-full rounded-lg px-3 py-2 text-left text-xs text-ink hover:bg-slate-50" onClick={() => action === "View Details" ? onView(item) : action === "Edit" ? onEdit(item) : onAction(item, action)}>{action}</button>)}</div></details></TableCell></TableRow>)}</TableBody></Table>; }
